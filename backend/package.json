{
  "name": "ecoride-backend",
  "version": "1.0.0",
  "description": "EcoRide backend server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-rate-limit": "^6.7.0",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}